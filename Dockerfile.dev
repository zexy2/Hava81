# Development Dockerfile
FROM node:26-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Keep the development image reproducible and match the root/CI peer policy.
RUN npm ci --legacy-peer-deps

# Copy source code
COPY . .

# Expose development port
EXPOSE 3000

# Start development server
CMD ["npm", "start"]
