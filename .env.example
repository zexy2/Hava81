# Only the API process reads this secret. Never prefix it with REACT_APP_.
OPENWEATHER_API_KEY=YOUR_OPENWEATHER_API_KEY

# Fastify API
PORT=4000
CORS_ORIGINS=http://localhost:3000

# Browser-facing BFF address. Relative /api/v1 uses the CRA/Nginx proxy.
REACT_APP_API_BASE_URL=/api/v1
