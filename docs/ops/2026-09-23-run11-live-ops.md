# Hava81 live operations checkpoint — 2026-09-23 21:40 TRT

## Verified production state

- Frontend/main revision: `49fc4d53a8b45e187ba51cfbaff93a5122c2f1a1`
- Public frontend: healthy (`HTTP 200`)
- Public API readiness: healthy (`HTTP 200`, `cache-control: no-store`)
- Active API proxy: **4002**
- Rollback/canary API: **4001** retained
- CORS: `https://hava81.zekiakgul.dev` allowed
- OpenWeather provider circuit: `closed`
- Boot assets: all expected city assets present
- Root disk: **90.8% used**, approximately **4.46 GiB free**; pressure warning persists, but build headroom is currently sufficient

## Release safety

- Deployed API revision remains `d8445e8af156a147d888bf64efbeabb3dc8c66c5`.
- Current `main` is newer, so API promotion remains **fail-closed**. No restart, port switch, or production API mutation was performed.
- Any API promotion must continue to use a validated 4001 canary before 4002 stable rollout.

## Browser-flow blocker

- PR #1202 remains open with Browser flows failures caused by stale `aria-current="location"` expectations in `e2e/smoke.spec.ts`.
- The bounded test-only fix exists locally at commit `e5df9631e405989e9fcf58bd575ad0fde1959a29`, but Oracle host Git HTTPS authentication is unavailable, so that local commit is not represented as a remote branch.
- Pending PR branches were not mutated.

## Next queue

1. Publish the isolated E2E contract fix through an authenticated GitHub write path when available.
2. Re-run exact-head CI and merge only after Browser flows, CodeQL, and deployment checks are green.
3. Re-verify SentinelX state immediately before any merge/deploy/rollback.
4. Continue independent non-API work while external jobs are pending; preserve the 4002/4001 topology and MGM/Open-Meteo trust boundaries.
